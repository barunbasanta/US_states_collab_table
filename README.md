# Collaboration Table Generator

Generates:
- Red, Green, Blue two-column collaboration tables
- Red, Green, Blue Massachusetts (Non KI) legend rows
- PNG and PDF for each output, bundled in one ZIP

Input columns:
`State`, `P`, `G`, `T`, optional `Total`.

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```

# Collaboration Table Generator

This fixed version uses Bokeh's built-in US state outline data, so state icons render as real map shapes instead of fallback colored boxes.

Input columns: `State`, `P`, `G`, `T`, optional `Total`.

Run:

```bash
pip install -r requirements.txt
streamlit run app.py
```
